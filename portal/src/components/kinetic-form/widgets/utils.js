import { closeConfirm, openConfirm } from '../../../helpers/confirm.js';
import {
  clearToasts,
  toastError,
  toastSuccess,
} from '../../../helpers/toasts.js';

export default {
  // Toasts
  toastSuccess,
  toastError,
  clearToasts,
  // Confirm modal
  openConfirm,
  closeConfirm,
};
